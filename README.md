# BedDepthCsvGenerator
Wrapper script that generates a CSV file of read depths based on BAM files and a BED file

Requires Perl
Requires Bedtools ver. 2.15.0 or higher installed
Requires Samtools

Works only on a UNIX environment
